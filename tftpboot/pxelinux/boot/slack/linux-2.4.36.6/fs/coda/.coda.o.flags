ifeq (-e stext psdev.o cache.o cnode.o inode.o dir.o file.o upcall.o coda_linux.o symlink.o pioctl.o sysctl.o,$(strip $(subst $(comma),:,$(LDFLAGS) $(EXTRA_LDFLAGS) $(obj-y))))
FILES_FLAGS_UP_TO_DATE += coda.o
endif
